## synthesisr 0.4.0

- full clean-up of import functions
- update `testthat` syntax
- move to `pkgdown` for docs, with new logo
- Many minor bug fixes


## synthesisr 0.3.0

This is a minor rebuild to meet modern data standards - mainly supporting 
`tibbles` rather than `data.frames` as a default
